spring.application.name=edu2

# DB설정
spring.datasource.url= jdbc:mysql://localhost:3306/ictedu_db
spring.datasource.username= ictedu
spring.datasource.password= 1111
spring.datasource.driver-class-name= com.mysql.cj.jdbc.Driver

# 생략 가능
server.port= 8080

mybatis.mapper-locations=mapper/*.xml
mybatis.type-aliases-package=com.ict.edu2
# mybatis.type-aliases-package=com.ict.edu2.domain    vo 들에 대한 별칭을 쓰기 위해 상위 폴더를 지정한다 (하위 폴더에 모두 적용/읽는다)